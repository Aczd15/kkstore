# KKStore (PHP + MySQL)

Готовый учебный проект интернет-магазина и сервисного центра техники.

## Возможности

- Многостраничный сайт: главная, каталог, услуги, ремонт, аксессуары, новости, о нас, контакты.
- Каталог товаров с фильтрацией по категории, бренду и цене.
- Регистрация и авторизация пользователей со **сложной CAPTCHA** (код + математическое выражение).
- Роли: `customer`, `manager`, `admin`.
- Профиль пользователя: загрузка аватара, смена пароля, история заявок на ремонт.
- Панель менеджера: отправка заявок на добавление товаров.
- Админ-панель:
  - добавление категорий, новостей, товаров;
  - добавление изображений для товаров;
  - модерация заявок менеджеров;
  - обработка заявок на ремонт (принять/отклонить **с обязательной причиной**);
  - модерация отзывов покупателей.
- Форма записи обычного пользователя на ремонт телефона и другой техники.
- Система отзывов и рейтингов товаров с модерацией администратором.
- Современный минималистичный дизайн (темная тема) + объемный контент на страницах.

## Запуск через XAMPP

1. Скопируйте проект в `htdocs`:
   - `C:\xampp\htdocs\kkstore`
2. Запустите Apache и MySQL в XAMPP Control Panel.
3. Создайте БД и таблицы:
   - Откройте `http://localhost/phpmyadmin`
   - Импортируйте файл `sql/schema.sql`
4. Проверьте настройки в `config/config.php`:
   - по умолчанию: host `127.0.0.1`, user `root`, password пустой, db `kkstore`
5. Проверьте, что папка `uploads/` доступна на запись.
6. Откройте сайт:
   - `http://localhost/kkstore/index.php?page=home`

## Тестовые аккаунты

- Администратор:
  - Email: `admin@kkstore.local`
  - Пароль: `admin123`
- Менеджер:
  - Email: `manager@kkstore.local`
  - Пароль: `admin123`

## Структура

- `index.php` — роутинг и обработка форм.
- `pages/` — страницы сайта.
- `templates/` — общий layout (header/footer).
- `includes/` — БД, авторизация, хелперы.
- `assets/` — стили и JS.
- `uploads/` — загружаемые аватары и изображения товаров.
- `sql/schema.sql` — готовая MySQL схема и демо-данные.

